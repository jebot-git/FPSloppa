"""Summarize the completed local-map traversal audit; contains no map assets."""
from pathlib import Path
from datetime import datetime, timezone
import json,subprocess
ROOT=Path(__file__).resolve().parents[1];OUT=ROOT/'test-results/converted-traversal'
paths=sorted((ROOT/'maps').glob('ad_arena_*.bsp'))+sorted((ROOT.parent/'Builds/TF-Local-Validated/maps').glob('*.bsp'))+sorted((ROOT.parent/'Builds/ThreeWave-Local/maps').glob('*.bsp'))
rows=[json.loads((OUT/(p.stem+'.json')).read_text()) for p in paths]
(OUT/'summary.json').write_text(json.dumps(rows,indent=2)+'\n')
counts={key:sum(len(r.get(key,[])) for r in rows) for key in ['spawns','water','doors','lifts','teleports','push_triggers']}
passed=sum(not r.get('failures') and not r.get('engine_errors') and r.get('exit_code')==0 for r in rows)
lines=['# Converted-map traversal audit','',f"Run: {datetime.now(timezone.utc).strftime('%Y-%m-%d %H:%M UTC')}. Godot 4.7.2, Linux headless physics, current working tree based on `{subprocess.check_output(['git','rev-parse','--short','HEAD'],cwd=ROOT,text=True).strip()}`.",'',f"Completed {len(rows)} maps; {passed} passed the sampled runtime checks. Coverage: {counts['spawns']} spawn capsules with settling/jumping/directional movement, {counts['water']} liquid samples, {counts['doors']} doors, {counts['lifts']} lifts, {counts['teleports']} teleport triggers and {counts['push_triggers']} push triggers.",'', 'All BSPs stayed in their existing local directories. No AD, original TF or ThreeWave BSPs are included in source changes. Per-map hashes, source paths, entity identifiers, coordinates, collision observations and engine logs are in `test-results/converted-traversal/`.', '', '## Checks and limits','', '- Actual character physics exercises spawn settling, jump input, directional motion, swimming, downward strokes, surface exits and dry-state reset. Surface-exit cases require an unobstructed capsule sweep and sufficient air above the water. Low ceilings are recorded as geometry constraints, not failed swimming.','- Every imported door is opened through production proximity activation, checked at the open endpoint and returned closed. Collision probes use actual trailing brush surfaces, offset slightly from the triangle plane, and check that they move and return. Earlier probes are reset so swimming cannot leave a door open at the start of its test. This does not prove every room-to-room route is usable.','- Lifts run through the production arena tick, with standing passengers through ascent and descent. A nine-point surface search distinguishes real lift support from the static floor below it.','- Teleport tests physically overlap imported Areas, then check authoritative destination, spawn serial and destination capsule clearance. Adjacent portals are approached from a point touching only the intended trigger. Push triggers must apply velocity.','- Fresh brush collision alignment is checked before deliberately restoring legacy identity transforms; runtime repair must then pass the physical checks. This exercises both fresh imports and compatibility with old scene caches.','- This is a headless physics audit, not a full-match route playtest or VR rendering/performance test. Portal artwork, animation and headset presentation are not visually certified.','', '## Fixes found during testing','', '1. Rotated brush collision did not receive the inverse entity rotation applied to its visible mesh. This displaced collision on `angle -1` lifts and other angled brushes. The importer now aligns both; the runtime also repairs existing caches. Before the fix, all four lifts in ThreeWave ctf2m2 and all eight in ctf2m5 failed the passenger test.','2. Teleport destinations omitted Quake’s 27-unit upward adjustment before converting to player feet coordinates, causing solid overlaps at several ThreeWave destinations. The adjustment is now applied. The behavior is defined in [id Software’s original teleport destination function](https://github.com/id-Software/Quake/blob/master/QW/progs/triggers.qc#L414).','', '## Compatibility that remains simplified','', 'The 16 AD conversions contain static `func_wall` / `func_illusionary` geometry but no active doors, lifts or teleport triggers. Their conversion readmes explicitly say gates and single-player scripts were removed. Passing movement checks does not restore those features.','', 'TF and ThreeWave doors use proximity opening; secret doors use a single slide rather than Quake’s two-stage movement. Lifts use a timed cycle. Original button/trigger target chains, secrets and map-specific team access logic are not interpreted. These are compatibility limitations, even where physical movement passes. Unsupported entity details remain in each JSON report.','', '## Per-map results','', '| Map | Spawns | Water samples | Doors | Lifts | Teleports | Result |','| --- | ---: | ---: | ---: | ---: | ---: | --- |']
for r in rows:
 ok=not r.get('failures') and not r.get('engine_errors') and r.get('exit_code')==0
 lines.append('| '+r['name']+' | '+' | '.join(str(len(r.get(k,[]))) for k in ['spawns','water','doors','lifts','teleports'])+' | '+('Pass' if ok else 'Investigate')+' |')
issues=[(r['name'],r.get('failures',[])+r.get('engine_errors',[])) for r in rows if r.get('failures') or r.get('engine_errors')]
if issues:
 lines+=['','## Outstanding failures','']
 for name,errors in issues:
  for error in errors:lines.append(f'- `{name}`: {error}')
  row=next(r for r in rows if r['name']==name)
  for lift in row.get('lifts',[]):
   if lift.get('boarded') and not lift.get('carried'):
    lines.append(f"  Lift `{lift['entity'].get('model')}` at `{lift['start']}`: requested travel {lift['travel']:.2f} m; rider rose {lift['rider_rise']:.2f} m; overhead capsule sweep allows {100*lift.get('overhead_clear_fraction',1):.1f}% of travel. The actor hits map geometry while the platform continues; a full standing ride is not certified.")
lines+=['','## Reproduction','', '`python3 tools/validate_converted_traversal.py` runs all available retained conversions. `--match threewave_ctf2m2` selects one map. Run `python3 tools/report_converted_traversal.py` afterward to regenerate this report. Requires the locally retained BSPs and Godot 4.7.2; it does not download or republish assets.','', 'Related regressions: `deathmatch/tests/quake_movement.gd`, `deathmatch/tests/water.gd`, and `python3 deathmatch/tests/run_network_tests.py --bsp` (local ENet server and two clients on the base LibreQuake map; this network smoke test is separate from the converted-map physics sweep).','']
dest=ROOT/'docs/CONVERTED_MAP_TRAVERSAL.md';dest.parent.mkdir(exist_ok=True);dest.write_text('\n'.join(lines));print(f'{passed}/{len(rows)} maps passed; {dest}')
